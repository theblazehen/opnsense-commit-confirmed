# JunOS like `commit confirmed`

Run `./commit confirmed <min>` then do your changes. If you do not run `./commit confirm` before <min> then the previous config will be restored and OPNSense rebooted
